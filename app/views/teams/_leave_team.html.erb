<div class="well-small" style="background-color: lavender;">
	<%= form_for @team, :url=>{:action=> 'leave'},:html => { :method => :delete } do |f| %>
	<%= f.submit t("teams.leave_team"), class: "btn btn-large btn-danger" %>
	<% end %>
</div>
